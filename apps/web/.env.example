# Copy to .env for local dev. NEVER commit .env.
# PRODUCTION: point at the live API, e.g. https://api.kabarbers.example.com
VITE_API_URL=http://127.0.0.1:8000
# Optional: override the homepage hero image without an owner upload.
# VITE_HERO_IMAGE_URL=
